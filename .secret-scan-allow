# harbinger .secret-scan-allow
# This repo's SecretPatternMatcher domain class + its tests embed example key SHAPES by design
# (harbinger detects secrets at runtime). Allowlist those so the git-level guard ignores its own fixtures.
# NOTE: one rule per line, no inline comments after a rule (the parser takes the whole line).
path:**/test/**
path:**/SecretPatternMatcher.java
